import React from 'react';
import ReactDOM from 'react-dom';
import App from './App/App';
import LoginPage from './Login/LoginPage';

ReactDOM.render(
  <App />,
  document.getElementById('root') // root is in public/index.html
);
